[build]
  base = "gTracker"
  command = "npm run build"
  publish = "dist"

# Client-side routing (React Router) - send all paths to index.html
# so refreshing or linking directly to e.g. /history doesn't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
